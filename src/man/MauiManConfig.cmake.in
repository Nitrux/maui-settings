@PACKAGE_INIT@

include(CMakeFindDependencyMacro)
find_dependency(Qt5Core @REQUIRED_QT_VERSION@)

# Any changes in this ".cmake" file will be overwritten by CMake, the source is the ".cmake.in" file.

foreach(PKG IN LISTS MauiMan_FIND_COMPONENTS)
    find_package(MauiMan${PKG})
    if(MauiMan_FIND_REQUIRED_${PKG})
        set_package_properties(MauiMan${PKG} PROPERTIES TYPE REQUIRED)
    else()
        set_package_properties(MauiMan${PKG} PROPERTIES TYPE OPTIONAL)
    endif()

    set(MauiMan_${PKG}_FOUND ${MauiMan${PKG}_FOUND})
endforeach()

include("${CMAKE_CURRENT_LIST_DIR}/MauiManTargets.cmake")

set(MauiMan_INSTALL_PREFIX "@PACKAGE_CMAKE_INSTALL_PREFIX@")

@PACKAGE_INCLUDE_QCHTARGETS@
